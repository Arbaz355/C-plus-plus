#include <stdio.h>
#include <conio.h>
int main ()
{
	int i,fact,c;
	printf("Enter the number to print factorial:");
	scanf("%d", &fact);
	for (i=1; i<=fact; ++i)
	{
		fact*i;
		printf("%d", c);
	}
	getch();
}
