#include <stdio.h>
#include <conio.h>
int main()
{
	int A,E,I,O,U,a,e,i,o,u,chacter;
	printf("Enter any chacter");
	scanf("%d", &chacter);
	if(chacter==A||E||I||O||U)
	{
		printf("Chacter is vowel");
	}
     if (chacter=a||e||i||o||u)
    {
    	printf("chacter is vowel");
	}
	else 
	{
		printf("chacter is consonant");
	}
	return 0;
}
